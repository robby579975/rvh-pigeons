const body=document.body;document.querySelector("[data-menu]")?.addEventListener("click",()=>body.classList.toggle("menu-open"));document.querySelectorAll(".nav a").forEach(a=>{const c=location.pathname.split("/").pop()||"index.html";if(a.getAttribute("href")===c)a.classList.add("active");a.addEventListener("click",()=>body.classList.remove("menu-open"))});const searchInput=document.getElementById("pigeonSearch");const familyFilter=document.getElementById("familyFilter");const cards=[...document.querySelectorAll(".pigeon-card")];function filterPigeons(){const q=(searchInput?.value||"").toLowerCase();const fam=familyFilter?.value||"";cards.forEach(card=>{const okText=card.dataset.search.includes(q);const okFam=!fam||card.dataset.family===fam;card.classList.toggle("hidden",!(okText&&okFam));});}searchInput?.addEventListener("input",filterPigeons);familyFilter?.addEventListener("input",filterPigeons);